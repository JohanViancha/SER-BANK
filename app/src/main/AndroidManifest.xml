<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ser_bank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SERBANK">
        <activity android:name=".Menu2"></activity>
        <activity android:name=".VisualizarTransacciones" />
        <activity android:name=".VisualizarUsuarios" />
        <activity android:name=".RealizarTransaccion" />
        <activity android:name=".CrearUsuario" />
        <activity android:name=".Menu" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>